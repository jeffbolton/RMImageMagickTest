Trying to use ImageMagick to use the gif encoding.  First I tried the pod:

## Using Pods

rake clean
rake pod:install
rake

Compiles fine, then on run it can't find some of the functions I am looking for.
Run into this:

app_delegate.rb:16:in `application:didFinishLaunchingWithOptions:': undefined method `MagickSetImageFormat' for #<AppDelegate:0x10ae46a00 ...> (NoMethodError)

## Using vendor
Documentation is confusing but I found that the pod is based on this: http://www.imagemagick.org/download/iOS/, so I tried the pre-compiled binaries. Still not finding stuff
Pre-compiled is in vendor/iOSMagick

Then I ompiled my own stuff from https://github.com/marforic/imagemagick_lib_iphone

Both co
